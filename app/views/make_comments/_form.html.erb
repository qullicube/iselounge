<%= form_for(@make_comment) do |f| %>
  <% if @make_comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@make_comment.errors.count, "error") %> prohibited this make_comment from being saved:</h2>

      <ul>
      <% @make_comment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
