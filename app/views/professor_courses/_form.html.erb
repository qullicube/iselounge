<%= form_for(@professor_course) do |f| %>
  <% if @professor_course.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@professor_course.errors.count, "error") %> prohibited this professor_course from being saved:</h2>

      <ul>
      <% @professor_course.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
