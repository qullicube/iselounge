<%= form_for(@professors_course) do |f| %>
  <% if @professors_course.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@professors_course.errors.count, "error") %> prohibited this professors_course from being saved:</h2>

      <ul>
      <% @professors_course.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
